import { connect }  from 'react-redux';


function Navbar() {
    return (
        <div>
            Navbar
        </div>
    )
}

const mapStateToProps = state =>({

})



export default connect(mapStateToProps,{

}) (Navbar)